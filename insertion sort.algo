 algorithm: insertion-sort(tab)
    for j=2 to tab.length
        new=tab[j]
        i=j-1
        while i>0 and tab[i]>new
            tab[i+1]= tab[i]
            i=i-1 
        tab[i+1]=new 
     end
 end